Prompt:
撰寫一個 Python 程式，該程式將建立一個基於 Dash 的股價預測網頁應用。具體要求如下：

讀取資料：

讀取名為 2330-training.csv 的 CSV 檔，並確保將日期列 (Date) 設為索引。
將資料中的特徵欄位（x1, x2, x3, x4, x5）中的逗號清除，並轉換為浮點數型別。

特徵組合與模型訓練：

計算所有特徵組合的均方誤差 (MSE)，使用線性回歸模型進行股價預測（y 為目標變數，x1 至 x5 為特徵）。
找出 MSE 最小的特徵組合，作為最佳組合進行預測。

建立網頁介面：

使用 Dash 建立應用，顯示最佳特徵組合、預測圖表，以及所有特徵組合的 MSE。
預測未來 30 天的股價並顯示其預測上下限。

功能要求：

圖表中顯示實際值、訓練預測值、測試預測值，並且要包括 95% 置信區間。
預測未來 30 天的股價並包含預測上下限。



