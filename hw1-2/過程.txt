過程：
版本 1：資料讀取與清理

將 CSV 檔載入程式，將 Date 設為索引，並進行資料清理。清除特徵欄位中的逗號，將數據轉換為浮點數格式。
使用 pandas 清理數據，建立初步資料框架。
進度更新：

完成了資料讀取和初步清理工作，資料轉換為可進行後續分析的格式。

版本 2：計算最佳特徵組合

使用 sklearn.linear_model.LinearRegression 建立多變量回歸模型，並計算所有特徵組合的 MSE。
使用 itertools.combinations 方法來創建特徵組合，並對每個組合進行模型訓練和測試，計算對應的 MSE。
進度更新：

完成了特徵組合的計算，並成功找出最佳的特徵組合。
目前所有特徵組合的 MSE 已準備好在網頁中顯示。

版本 3：建立 Dash 網頁應用

使用 Dash 建立應用，顯示最佳特徵組合和 MSE 資料表。
在圖表中繪製實際值與預測值，並包含 95% 預測區間（上下限）。
進度更新：

初步完成了 Dash 網頁介面的佈局，包括圖表和資料表的呈現。
現在能夠根據最佳特徵組合進行預測，並在圖表中呈現預測結果。

版本 4：預測未來股價

使用最佳特徵組合，預測未來 30 天的股價，並顯示未來預測上下限的區間。
將預測值加入現有的圖表，並新增一個區域顯示預測區間。
進度更新：

已成功預測未來 30 天的股價，並完成相關區域的繪製。
未來的預測值已顯示在圖表上，並包含上下限區域，顯示了股價的變化趨勢和不確定性。

版本 5：完善與測試

進行程式測試與優化，包括確保各種情境下的正確運行，如資料讀取錯誤處理、模型的健全性等。
測試了網頁應用的響應速度與互動性，確保用戶體驗順暢。
進度更新：

程式碼的錯誤處理和優化工作完成，現在應用運行穩定。
所有功能（資料顯示、模型計算、未來預測）都能正常工作。